# v2.1 管理後台（已完成）

## 目標

提供管理員儀表板與使用者管理功能。

## 功能

### 1. 管理員儀表板
- 總使用者數、已驗證/未驗證人數
- 線上人數（容器 running 數）
- 總磁碟用量
- 容器狀態一覽表
- 最近註冊/刪除紀錄（最近 10 筆）

### 2. 使用者管理
- 列表：信箱、驗證狀態、註冊時間、最後登入、磁碟用量
- 分頁：每頁 20 筆，可搜尋、排序
- 啟用/停用帳號
- 手動重設密碼（同步到容器）
- 查看個別使用者容器狀態

### 3. 配額系統
- 每個使用者配額（預設 10GB）
- 磁碟用量追蹤（寫入/刪除時更新）
- 超過配額時阻擋上傳，回傳 507 Insufficient Storage
- 管理員可調整配額

### 4. 容器管理
- 列出所有容器狀態
- 手動重啟容器
- 手動刪除容器
- 查看容器日誌

## 資料庫變更

```sql
ALTER TABLE users ADD COLUMN quota_gb INTEGER DEFAULT 10;
ALTER TABLE users ADD COLUMN disk_usage_bytes INTEGER DEFAULT 0;
ALTER TABLE users ADD COLUMN is_admin INTEGER DEFAULT 0;
ALTER TABLE users ADD COLUMN is_active INTEGER DEFAULT 1;
ALTER TABLE users ADD COLUMN last_login TEXT;
```

## API 端點

| Method | Endpoint | 說明 |
|--------|----------|------|
| GET | `/api/admin/dashboard` | 儀表板統計 |
| GET | `/api/admin/users` | 使用者列表 |
| GET | `/api/admin/users/{id}` | 使用者詳情 |
| PUT | `/api/admin/users/{id}` | 更新使用者（配額、啟用） |
| DELETE | `/api/admin/users/{id}` | 刪除使用者 |
| POST | `/api/admin/users/{id}/reset-password` | 重設密碼 |
| GET | `/api/admin/containers` | 容器列表 |
| POST | `/api/admin/containers/{name}/restart` | 重啟容器 |

## 前端頁面

- `/admin` — 管理員儀表板
- `/admin/users` — 使用者管理
- `/admin/users/{id}` — 使用者詳情
- `/admin/containers` — 容器管理

## 變更檔案

| 檔案 | 說明 |
|------|------|
| `k8s/main.py` | 新增 admin API、quota middleware |
| `k8s/templates/admin/` | 管理員頁面（新增） |
| `k8s/templates/admin/users.html` | 使用者列表 |
| `k8s/templates/admin/dashboard.html` | 儀表板 |
| `k8s/middleware.py` | 配額檢查中介層（新增） |

## 預估工時：3-4 天